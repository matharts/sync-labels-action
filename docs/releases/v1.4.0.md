# v1.4.0

`v1.4.0` 将 Action 迁移到 GitHub 原生 Node.js 24，并在整次运行开始写入前提供删除安全保护。

## 升级

- 使用方应审核发布提交，并使用 README 中的完整提交 SHA；不要固定到可移动版本标签。
- 标签清单、策略文件格式、Action inputs 和 outputs 均无需迁移。
- 项目维护者可以在 `safety` 中显式配置删除行为和数量上限；省略该字段仍保持 v1.3 行为。

## 兼容性保证

- 默认仍为 dry-run。
- v1.3 的 inputs、outputs 名称和含义保持不变。
- 未配置 `safety` 时仍允许删除且不设置数量上限，与 v1.3 相同。
- 读取请求保持可重试；写入请求不会自动重试。
- 规划、部分失败计数、Unicode 处理和任务摘要继续由 Ruby v1.3 行为快照验证。

## 删除安全

- 所有目标仓库会在首个写请求前完成规划并汇总为不可变 `RunPlan`。
- `safety.deletions: deny` 禁止任何删除。
- `safety.max_deletions_per_repository` 限制单仓库计划删除量。
- `safety.max_deletions_total` 限制整次运行计划删除总量。
- 任一规则阻止执行时，写请求数为零；任务摘要会展示触发规则、删除总量和受影响仓库。
- MathArts 生产策略使用 `allow / 1 / 6`，依据是当前 6 个合格仓库的零删除基线。

## 生产验证

- [RC 全组织 dry-run](https://github.com/matharts/sync-labels-action/actions/runs/29418568348)
  精确检出 `v1.4.0-rc.1`，覆盖 6 个仓库且计划删除为 0。
- [Node.js 24 单仓库真实写入](https://github.com/matharts/sync-labels-action/actions/runs/29418676799)
  仅更新 1 个受控颜色漂移，创建、重命名、删除和失败计数均为 0。
- [紧随其后的 RC dry-run](https://github.com/matharts/sync-labels-action/actions/runs/29418792623)
  输出 `changed=false`，所有变更和失败计数均为 0。
- GitHub App token 和私钥在日志中被掩码，临时 token 在作业结束后撤销。

## 已知限制

- dry-run 不会被删除上限阻止；它会完整展示计划，维护者必须在写入前审查删除量。
- `repositories.exclude`、离线 `validate_only`、类型化错误和可归档计划文件不在本版本范围内。

## 验证

- `pnpm check`
- Node.js 24 持续集成
- 可复现 `dist/index.js` bundle 比对
- Actionlint workflow 校验
- CodeQL
- 全组织 dry-run、单仓库真实写入和零差异复查
